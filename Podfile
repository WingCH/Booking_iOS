# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Booking' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Booking
   pod 'Alamofire', '~> 4.5'
   pod 'SwiftyJSON'
   pod 'SwiftMoment'
   pod 'CalendarKit'
   pod 'PopupDialog', '~> 0.6'
   pod 'Kingfisher', '~> 4.0'
   pod "EFQRCode", '~> 4.0'
   pod 'SwipeCellKit'
   pod 'CRRefresh'

  target 'BookingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BookingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
